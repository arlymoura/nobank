<div class="card events-card">
  <header class="card-header">
    <p class="card-header-title">
        Saque Efetuados
    </p>
    <a href="#" class="card-header-icon" aria-label="more options">
    <span class="icon">
    <i class="fa fa-angle-down" aria-hidden="true"></i>
    </span>
    </a>
  </header>
  <div class="card-table">
      <div class="content">
          <table class="table is-fullwidth is-striped">
              <tbody>
                <% @extract_withdrawals.each do |extract_withdrawal| %>
                  <tr>
                    <td width="5%"><i class="fa fa-bell-o"></i></td>
                    <td><%= formated_date(extract_withdrawal.created_at) %></td>
                    <td>Saque Efetuado</td>
                    <td class="level-right"><a class="button is-small is-primary" href="#">Valor: <%= extract_withdrawal.value %></a></td>
                  </tr>
                <% end %>
            </tbody>
          </table>
      </div>
  </div>
</div>