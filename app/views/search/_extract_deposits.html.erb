<div class="card events-card">
  <header class="card-header">
    <p class="card-header-title">
        Depósitos Efetuados
    </p>
    <a href="#" class="card-header-icon" aria-label="more options">
    <span class="icon">
    <i class="fa fa-angle-down" aria-hidden="true"></i>
    </span>
    </a>
  </header>
  <div class="card-table">
      <div class="content">
          <table class="table is-fullwidth is-striped">
              <tbody>
                <% @extract_deposits.each do |extract_deposit| %>
                  <tr>
                    <td width="5%"><i class="fa fa-bell-o"></i></td>
                    <td><%= formated_date(extract_deposit.created_at) %></td>
                    <td>Depósito Efetuado</td>
                    <td class="level-right"><a class="button is-small is-primary" href="#">Valor: <%= extract_deposit.value %></a></td>
                  </tr>
                <% end %>
            </tbody>
          </table>
      </div>
  </div>
</div>