<%= form_with(model: [@account, transfer], local: true) do |f| %>
  <% if transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
        <% transfer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div >
  <div class="field">
    <label class="label">Numero da Conta</label>
    <div class="control">
      <%= f.text_field :account, class: 'input', placeholder: "Digite Numero da Conta" %>
    </div>
  </div>
  <div class="field">
    <label class="label">Valor Transferido</label>
    <div class="control">
      <%= f.number_field :value, step: :any, min: 1, class: 'input' %>
    </div>
  </div>
  <div class="control">
    <%= f.submit 'Efetuar Transferência', class: "button is-primary"  %>
  </div>
<% end %>
